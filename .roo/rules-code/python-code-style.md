* Always end the file with the new line.
* Place 2 empty lines between top-level declarations (methods, var blocks, imports)
* Use 'modern' type hints, like `list` (not `List`), `str|None` (not `Optional[str]`)
